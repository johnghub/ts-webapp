{
  "name": "ai-proto",
  "version": "1.0.0",
  "description": "AI generated site",
  "scripts": {
    "test": "jest",
    "dev": "vite",
    "build": "npm run clean && vite build",
    "build:copy": "npm run clean:server && npm run build && npm run copyto:server",
    "copyto:server": "copyfiles -u 1 dist/index.html dist/assets/*  ../server/Web.Api/Web.Api/wwwroot",
    "clean:server": "rimraf ../server/Web.Api/Web.Api/wwwroot",
    "clean": "rimraf ./dist",
    "preview": "vite preview"
  },
  "author": "JMH",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "copyfiles": "^2.4.1",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5",
    "vite": "^5.2.8"
  }
}
